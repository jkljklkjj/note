1. 如何做到账号去重
如果是自增整数，直接用bitmap。如果是uuid或者不自增的整数，那就用多个key或者set存
2. 如果是uuid或者不是自增整数怎么存
3. 如何找到已有的所有账号
自增整数很简单用bitmap遍历，如果是大整数也可以考虑用一下。那set就可以用sscan，或者key也可以用scan。但是这样性能可能不高
4. 如果聊天室改造成分布式，如何保证分布式一致性
这里不一致性会体现在哪里
5. 用二级索引查询时，哪里会加锁
会加行锁。如果回表的过程中查找对应的主键的时候，主键不能被修改，否则查找的数据可能发生不一致。所以会给匹配二级索引的行加上行锁