#### 推理速度上，INT8 和 FP16 比起来怎么样？

一般INT8更省显存、吞吐更高；FP16精度通常更稳、生态更成熟。真实收益取决于硬件对INT8算子的支持程度

#### 大模型有推理能力吗？

有，但不是严格符号推理。它能完成多步推断、代码和数学推导，但仍可能逻辑出错或幻觉，因此需结合工具与校验机制

#### 大模型生成时的参数怎么设置？

常用参数：temperature、top_p、top_k、max_new_tokens、repetition_penalty、stop。经验：

- 事实问答：低温（0.2~0.7）
- 创意生成：高温（0.8~1.1）

#### 有哪些省内存的大语言模型训练/微调/推理方法

- 量化（8bit/4bit）
- LoRA/QLoRA
- 梯度检查点
- ZeRO/FSDP分片训练
- 推理时KV Cache优化、PagedAttention（如vLLM）

#### 介绍一下deepseek网页版的完整推理过程

##### 浏览器端

```js
// 1. 你输入 "介绍一下DeepSeek"
// 2. 前端将文本转换成 token IDs
//    "介绍" → [12345]
//    "一下" → [67890]
//    "DeepSeek" → [13579, 24680]

// 3. 构造请求体
const request = {
    messages: [
        { role: "user", content: "介绍一下DeepSeek" }
    ],
    stream: true,           // 开启流式输出
    temperature: 0.7,
    max_tokens: 4096,
    model: "deepseek-chat"
};

// 4. 通过 WebSocket 或 SSE 发送
ws.send(JSON.stringify(request));
```

##### 网关

```python
# 伪代码
class Gateway:
    def handle_request(request):
        # 1. 验证 API Key / 用户登录态
        if not authenticate(request.api_key):
            return 401
        
        # 2. 检查用户配额（免费用户每天多少条）
        if user.daily_usage >= quota:
            return 429
        
        # 3. 限流（防止恶意刷接口）
        if rate_limiter.is_over_limit(user_id):
            return 429
        
        # 4. 路由到推理集群
        return route_to_inference_cluster(request)
```

##### 预处理

网页版和 API 版的系统提示词不同。网页版更“友好”，API 版更“中性”

```python
# DeepSeek 网页版有隐藏的系统提示词
# 你直接看到的是用户消息，但实际上发送给模型的是：

FULL_PROMPT = """你是DeepSeek，由深度求索公司创造的AI助手。你的知识截止日期是2025年5月。你的回答要准确、有帮助、无害。

用户的问题是：介绍一下DeepSeek

请回答："""
```

#### 模型推理

```
输入 tokens
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│  第 1 层 Transformer                                     │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Router（路由器）                                  │  │
│  │  - 分析每个 token 属于哪个“专家”                   │  │
│  │  - 激活 8 个专家中的 2-3 个                        │  │
│  └───────────────────────────────────────────────────┘  │
│       │              │              │                    │
│       ▼              ▼              ▼                    │
│  ┌────────┐    ┌────────┐    ┌────────┐                │
│  │专家 A  │    │专家 C  │    │专家 F  │                │
│  │(代码)  │    │(推理)  │    │(知识)  │                │
│  └────────┘    └────────┘    └────────┘                │
│       │              │              │                    │
│       └──────────────┼──────────────┘                    │
│                      ▼                                    │
│              加权融合输出                                 │
└─────────────────────────────────────────────────────────┘
    │
    ▼
重复 61 层（DeepSeek-V3 有 61 层 MoE）
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│  输出层：Softmax 转概率分布                              │
│  "DeepSeek是深度求索公司推出的AI模型..."                │
└─────────────────────────────────────────────────────────┘
```

##### 流式输出

```python
# 模型内部生成过程
token1 = model.generate("介绍一下DeepSeek")  # "Deep"
token2 = model.generate("Deep")              # "Seek"
token3 = model.generate("DeepSeek")          # "是"
token4 = model.generate("DeepSeek是")        # "深"
token5 = model.generate("DeepSeek是深")      # "度"
# ... 一直持续到生成结束标记

# 每生成一个 token，立即通过 WebSocket 发送
ws.send({"delta": "Deep"})
ws.send({"delta": "Seek"})
ws.send({"delta": "是"})
```